import { NavBar } from "@/components";

const Page = () => {
  return (
    <main className="flex w-screen h-screen justify-center items-center">
      <NavBar />
    </main>
  );
};

export default Page;
